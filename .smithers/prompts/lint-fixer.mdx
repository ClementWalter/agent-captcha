`trunk check --fix` already auto-applied every fix it could. The output
below is what it could not auto-fix. Resolve each remaining issue by
editing the relevant file. Keep changes minimal — fix only what trunk
flagged, do not refactor or reformat unrelated code.

If a flagged issue cannot be safely fixed (e.g. a real bug behind the
lint rule, a false positive that would need a config change), record it
in the action with a short note explaining why instead of editing.

Trunk output (only files/issues that need attention — auto-fixed ones
are omitted):

{props.trunkOutput}
